import { CardDescription, CardHeader, CardTitle } from "../ui/card";

export default function CreateAcoountCardHeader() {
  return (
    <CardHeader>
      <CardTitle>Join us today!</CardTitle>
      <CardDescription>
        We were looking for people like you to join.
      </CardDescription>
    </CardHeader>
  );
}
